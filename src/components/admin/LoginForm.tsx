// src/components/admin/LoginForm.tsx
// Este componente não é mais necessário pois o login foi removido.
// O conteúdo foi removido para evitar uso acidental.

export default function LoginForm() {
  return null;
}
