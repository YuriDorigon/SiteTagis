// src/app/adm/dashboard/convenios/page.tsx
import ManageConvenios from '@/components/admin/ManageConvenios';

export default function AdminConveniosPage() {
  // In a real app, data fetching for convenios would happen here or in ManageConvenios server-side
  return <ManageConvenios />;
}
